## Sample certificate automater

Required packages
- pillow "pip install pillow pandas"
- opencv "pip install opencv-python"

How to run the code ?
- "python main.py" in terminal.

The generated certificate will be available in /out directory

